<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
android:id="@+id/activity_container"
android:layout_width="match_parent"
android:layout_height="match_parent"
tools:context="com.mastercoder.rutas_mcturistic.MainActivity">

<FrameLayout
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/bottombar_height"
    >

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/btnLogOut"
        android:layout_width="62dp"
        android:layout_height="52dp"
        android:clickable="true"
        app:fabSize="mini"
        android:background="@android:color/white"
        app:rippleColor="@color/wallet_link_text_light"
        app:srcCompat="@drawable/cast_ic_notification_disconnect"
        android:layout_marginTop="30dp"
        android:layout_marginRight="20dp"
        android:layout_gravity="right"/>



    <android.support.design.widget.FloatingActionButton
        android:id="@+id/Recomendador"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:src="@drawable/comentario"
        android:layout_gravity="bottom|left"
        android:background="@android:color/white"
        android:visibility="invisible"
        />
</FrameLayout>

<!--Agrega el layout para poder mostrar la vista-->
<include layout="@layout/buttombar"></include>

    <EditText
        android:id="@+id/Datetxt"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="invisible"
        />
</RelativeLayout>
